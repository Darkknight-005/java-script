/*
there are three type of operator 
1."delete" this operator delete object's property.
    delete object.property;
    delete object[propertyKey];
    delete objectName[index];

2."typeof" this operator tells us what type of value do operand contains
typeof()  inside the parentheses operand or expression.
3."void" The void operator specifies an expression to be evaluated without returning a value. expression is a      
            JavaScript expression to evaluate. The parentheses surrounding the expression are optional, but it is good style to use them. 
 */